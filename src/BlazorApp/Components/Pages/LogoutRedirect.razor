@page "/logout-redirect"
@using Microsoft.AspNetCore.Components
@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Redirect zur Logout-Endpoint
        Navigation.NavigateTo("/Account/Logout", true);
    }
}
